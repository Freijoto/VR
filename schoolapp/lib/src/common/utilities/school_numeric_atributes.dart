import 'package:flutter/material.dart';

class SchoolPadding {
  static const EdgeInsets xSmall = EdgeInsets.all(2.0);
  static const EdgeInsets small = EdgeInsets.all(4.0);
  static const EdgeInsets medium = EdgeInsets.all(8.0);
  static const EdgeInsets large = EdgeInsets.all(16.0);
  static const EdgeInsets xLarge = EdgeInsets.all(32.0);
}

class SchoolRadius {
  static const Radius xSmall = Radius.circular(2);
  static const Radius small = Radius.circular(4);
  static const Radius medium = Radius.circular(8);
  static const Radius large = Radius.circular(16);
  static const Radius xLarge = Radius.circular(32);
}

class SchoolBorderRadius {
  static BorderRadius xSmall = BorderRadius.circular(2);
  static BorderRadius small = BorderRadius.circular(4);
  static BorderRadius medium = BorderRadius.circular(8);
  static BorderRadius large = BorderRadius.circular(16);
  static BorderRadius xLarge = BorderRadius.circular(32);
}
